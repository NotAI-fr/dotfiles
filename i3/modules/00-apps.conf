# Application commands used by keybinds and startup.
# Edit this one file when substituting your preferred applications.

set $terminal kitty
set $file_manager nemo
set $browser helium-browser
set $lock_command betterlockscreen -l blur
set $startup_chat vesktop
